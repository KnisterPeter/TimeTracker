# TimeTracker #

TimeTracker is a simple web-based time and task manager. The user interface is based on the awesome <http://qooxdoo.org>.

## Setup ##

* `./configure.sh`
* `cd TimeTracker`
* `./generate.py build`
* Deploy the contents of the build folder to your webserver

